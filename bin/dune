(executable
 ; (public_name homepage)
 (name index)
 (modes js)
 ; (js_of_ocaml (flags --opt=3 --no-sourcemap --pretty --no-inline --debug-info))
 ; Explicitly requiring `digestif.ocaml` because `digestif.c` is automatically picked and makes js crash
 (libraries homepage lwt tyxml owl-base irmin digestif.ocaml)
 )
